[Unit]
Description=Noctalia shell (quickshell bar/wallpaper/lockscreen)
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Slice=session.slice
ExecStart=/usr/bin/qs -c noctalia-shell
Restart=on-failure
RestartSec=2

[Install]
WantedBy=graphical-session.target
